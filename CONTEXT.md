# ai-dev-radar

统计、管理并可视化展示多个研发团队 AI 研发效能的服务。团队是当前看板统计的数据归属最小单位；团队成员可以作为源数据责任人关联，个人度量属于后续扩展，不改变当前团队级看板。

## Language

### 数据归属

**团队（Team）**:
当前看板统计和数据权限的最小归属单位，由名称和数据源映射构成。团队成员可以关联源数据，但当前看板不按个人统计。
_Avoid_: 部门、小组、项目组

**团队成员（Team Member）**:
团队中的人员主数据，由姓名、工号和角色构成；工号是关联需求、问题单、MR 等源数据的稳定标识。人员关系用于责任归属和后续个人度量，不是当前看板的统计粒度。

**产品（Product）**:
团队负责的产品，是产品版本的直接归属对象。

**产品版本（Product Version）**:
产品下的可交付版本，形如 SCC 27.1.RC1 的版本标识。产品版本属于一个产品，关键研发活动可以按版本和迭代统计。

**开发迭代期（Iteration）**:
产品版本内的迭代阶段，形如 SCC 27.2.RC1-迭代一，隶属于某个产品版本。一个迭代可跨多个周/月；事实记录同时携带迭代标签和时间字段（如需求开始/完成时间），周/月视图由时间字段聚合。

**数据源映射（Source Mapping）**:
团队配置中描述“该团队的数据在源系统里对应哪些实体”的映射集合，如版本号、代码仓地址。自动采集按映射过滤，人工编辑按团队权限操作。

### 指标体系

**关键研发活动**:
IPD 研发流程中的八类核心活动：SA设计、SE设计、开发设计、编码开发、测试用例生成、测试用例执行、DTS缺陷分析、DTS缺陷修复。每类活动挂载若干度量指标。

**通用研发能力**:
跨活动复用的七类工程能力：MR代码检视、MR-E2E能力、CodeCheck问题修复、三方件治理、漏洞修复、自动化构建部署、白盒安全问题修复。

**渗透率**:
使用 AI 的数量占该活动总数的比率。分子 = 使用 AI 的数量，分母 = 总数，按周期统计。
_Avoid_: 使用率、覆盖率

**效率提升**:
工作量评估与实际投入的对比：(预估工作量 − 实际工作量) / 实际工作量，工作量单位为人天（人天是统一标准，不引入加权）。例：预估 10 人天、实际（使用 AI）5 人天，效率提升 100%。汇总时对预估、实际分别求和后代入公式。

**IR / AR / SR**:
需求分层（初始需求 / 分配需求 / 子需求），沿用 IPD 口径。SA设计与SE设计按 IR 度量渗透，开发设计与编码开发按 AR 度量渗透，测试用例生成按 SR 度量渗透。

**DTS**:
缺陷跟踪系统及其中的缺陷记录。DTS 缺陷分析与 DTS 缺陷修复是两个独立的关键研发活动。

**需求数据源（Requirement Source）**:
需求类业务记录的集合；IR、AR、SR 分别表示初始需求、分配需求和子需求，是需求内部层级，不是与需求并列的数据源。

**问题单数据源（Issue Source）**:
缺陷或问题跟踪记录的集合。当前导航承接历史 DTS 入口，具体字段和平台契约尚未定义。

**MR 数据源（MR Source）**:
合并请求记录的集合。当前字段、校验和采集契约尚未定义。

**代码检视数据源（Code Review Source）**:
独立代码检视记录的集合，与 MR 在导航上分开；两者的关联规则尚未定义。

**全公司均值（Company Average）**:
全部团队的事实记录合并计算出的均值，作为各团队趋势的对照基线，以弱化样式（灰）呈现在趋势图上。
_Avoid_: 平均线、基准值

**指标目录（Metric Catalog）**:
全部活动及其指标的声明式清单：每个指标声明所属活动、名称、类型（渗透率/效率提升/数量/布尔/比率）及分子分母的语义。新增指标即新增目录条目，而非修改代码。

**AI 属性（AI Attribute）**:
附着在源数据记录上的 AI 研发属性，例如是否使用 AI 辅助研发。属性可以来自自动采集或人工维护，并保留字段级来源和维护信息。

**AI 度量指标（AI Metric）**:
基于一个或多个数据域的有效源数据记录，按筛选条件和计算规则得到的度量结果。结果按查询实时计算，不作为可直接编辑的业务数据保存。

**成熟度评估记录（Maturity Record）**:
团队针对一个关键研发活动或通用研发能力在一个评估月维护的 0–5 分人工评估。记录独立于事实指标，不由指标自动推导。

**评估月（Assessment Month）**:
成熟度记录的月度归属，规范化为 YYYY-MM；新月份不会自动沿用上月，是否复制由维护者明确操作。

**成熟度等级（Maturity Level）**:
成熟度分值的 `floor` 等级：L0 未建设、L1 建设中、L2 试点中、L3 推广中、L4 规模使用、L5 成熟运营。领域平均先按已评估团队等权计算，再对未四舍五入平均值取 floor。

### 数据采集

**采集器（Collector）**:
对接一个内部系统平台并产出标准化源数据记录的适配器。获取方式可以是 CLI、MCP 或 API；当前无联调环境，仅预留接口设计。

**数据域（Data Domain）**:
具有独立记录结构和业务口径的一类研发数据。当前数据管理按需求、问题单、MR、代码检视四类业务数据源组织；IR、AR、SR 是需求内部分类。

**源数据记录（Source Record）**:
来自平台、导入或人工维护的具体业务记录，例如一条 IR 需求、一条问题单或一条 MR。源数据记录保存业务字段和 AI 属性，指标由其有效记录计算。

**临时数据（Staged Data）**:
尚未写入正式数据的导入或采集结果，经过整批校验和用户确认后才会更新正式数据。临时数据不参与看板计算。

**数据变更日志（Audit Log）**:
记录正式数据被人工编辑、导入确认或采集更新时的操作人、时间、来源和字段变化，用于追溯数据变化。

**事实记录（Fact Record）**:
一条"团队 × 指标 × 迭代（关键研发活动）或纯时间（通用研发能力）"的分子分母原始数，携带时间字段（需求开始/完成时间）、来源标记（自动采集 / 手动补录）、录入人与时间。所有率由看板侧基于事实记录计算，不存储平台吐出的现成比率。
当前事实记录是旧版看板的兼容数据形态；新数据管理工作台以源数据记录为主，后续通过适配层替换旧事实链路。

**数据补录（Manual Entry）**:
人工编辑或补充源数据记录，用于补充采集不到的字段和修正已确认的数据。页面编辑可以覆盖已有值；导入和采集确认只补正式记录中的空字段。
_Avoid_: 手动覆盖、导入
