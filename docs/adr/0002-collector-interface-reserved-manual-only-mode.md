# 采集器接口预留设计，全指标支持仅补录模式

当前开发环境无法访问公司内部系统平台做对接联调，因此采集器（对接一个内部平台的适配器，取数方式为 CLI/MCP/API）只定义接口、不做实现。所有指标在目录中可标记为"仅补录"，通过手动补录跑通全链路；联调环境就绪后再逐个接入采集器，不阻塞开发与验收。

## Consequences

- 第一版交付后数据全靠人工录入，需在补录页明确提示来源标记。
- 采集器接口一旦被多个实现依赖会趋于稳定，设计时按"输入=数据源映射+时间窗口，输出=事实记录"最小化。
