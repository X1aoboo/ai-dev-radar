# 产品版本按团队产品层级归属

产品版本不再直接与团队建立多对多归属，而采用“团队 → 产品 → 产品版本 → 开发迭代期”的层级。源数据记录通过产品、版本和迭代定位到团队；团队成员只负责责任人关联，不改变历史源数据的团队归属。

## Consequences

- 新增版本必须选择产品，新增迭代必须选择版本。
- 查询可以沿层级筛选团队、产品、版本和迭代。
- 旧版没有产品归属的演示事实数据继续由兼容看板使用，不承担历史迁移。
